# Environment Configuration for Medical AI Platform

# Session Secret (required for authentication)
SESSION_SECRET=your-secret-key-here

# OpenAI API Key (if using OpenAI for model fallback)
OPENAI_API_KEY=your-openai-api-key

# Local Model Paths
# RAG Model - Medical Q&A Model Path
RAG_MODEL_PATH=./models/rag-qa-model

# Medicine Recommendation Model Path  
RECOMMENDATION_MODEL_PATH=./models/recommendation-model

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (if using PostgreSQL for drug database)
# DATABASE_URL=postgresql://user:password@localhost:5432/medical_ai

# Model Server Endpoints (if models run as separate services)
# RAG_MODEL_ENDPOINT=http://localhost:8001
# RECOMMENDATION_MODEL_ENDPOINT=http://localhost:8002
